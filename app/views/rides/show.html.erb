<div style="margin-left: 50px; margin-right: 50px;">
	<center>
		<h2><%= @ride.name %></h2>
		<p style="width: 800px"><%= @ride.description %></p>
		<%= image_tag(@ride.posterfile)%>
		<br/>
		<p>Trip Duration: <%= (@ride.end-@ride.start) %>, From <%= @ride.start%></p>
		<br/>
		<p><%= @ride.details%></p>
	</center>
	
	<%= link_to 'Back', rides_path %>	
</div>

